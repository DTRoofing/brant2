# =============================================================================
# DOCKER ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains environment variable overrides for Docker containers
# =============================================================================

# Database - Use container service name
DATABASE_URL="postgresql://ADMIN:Brant01!@db:5432/postgres?sslmode=disable"

# Redis - Use container service name
CELERY_BROKER_URL="redis://redis:6379/0"
CELERY_RESULT_BACKEND="redis://redis:6379/0"

# Google Credentials - Container path
GOOGLE_APPLICATION_CREDENTIALS="/app/google-credentials.json"

# Upload directory - Container path
UPLOAD_DIR="/app/uploads"
UPLOAD_PATH="/app/uploads"

# Logs - Container path
LOG_FILE="/app/logs/backend.log"

# API URLs for container networking
FRONTEND_API_BASE_URL="http://api:3001/api"

# Tesseract - Already installed in container
TESSERACT_CMD="tesseract"

# Poppler - Already installed in container (poppler-utils)
POPPLER_PATH="/usr/bin"